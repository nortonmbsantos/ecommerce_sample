<div class="table-responsive-xl">
  <table class="table">
<thead>
<tr>
<th>Name</th>
<th colspan="3"></th>
</tr>
</thead>

<tbody>
<% @categories.each do |c| %>
<tr>
<td><%= c.name %></td>
<td>
<div class="btn-group float-right" role="group" aria-label="Basic example">
  <%= link_to 'Edit', edit_admin_category_path(c), class: 'btn btn-success float-right' %>
  <%= link_to 'Destroy', c, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger float-right' %>
</div>
</td>
</tr>
<% end %>
</tbody>    
  </table>
</div>
<br>

<%# Fazer paginação %>
<%= link_to 'New Category', new_admin_category_path, class: 'btn btn-primary' %>